import styles from "./hero.module.css";

const Hero = () => {
  return (
    <div className={styles.heroContainer}>
      <h1 className={styles.title}>Discover the Best Places to Eat</h1>
    </div>
  );
};

export default Hero;
